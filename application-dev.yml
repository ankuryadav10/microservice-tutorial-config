spring:
  application:
    name: CONFIG-DEV

eureka:
  instance:
    prefer-ip-address: true
  client:
    fetch-registry:
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka
